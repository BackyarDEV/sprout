info:
  name: Add Role
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/roles"
  body:
    type: json
    data: |-
      {
        "role": "Software Engineer I"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
