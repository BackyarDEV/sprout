info:
  name: Fetch Employees
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/employees"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
