info:
  name: Delete Employee
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseURL}}/employees/:id"
  params:
    - name: id
      value: "4"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
