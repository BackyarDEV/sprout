info:
  name: Fetch Teams by Name
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/teams/:team"
  params:
    - name: team
      value: Freelance
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
